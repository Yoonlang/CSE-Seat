version: '3'
services:
  nextjs:
    ports:
      - "80:80"
    volumes:
      - .:/usr/src/app
    environment:
      - TZ=Asian/Seoul
    build:
      context: .
      dockerfile: ./dockerfile

      
